# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MatrixKitClientTest' do
  
  # Pods for MatrixKitClientTest
  pod 'MatrixKit'
  
  # The GHMarkdownParser pod (a dependency of MatrixKit) does not support
  # dynamic frameworks. Uncomment the following line and run `pod install`
  # to trigger the error.
  #use_frameworks!

end
